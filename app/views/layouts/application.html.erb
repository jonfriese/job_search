<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "JobSearch" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <div class="navbar navbar-fluid-top">

      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>         
          <%= link_to JobSearch, root_path, html_options = {class: "brand"} %>
          <% if params[:keywords] && params[:location] %>
            <% if params[:keywords].empty? && params[:location].empty? %>
              <p class="brand"> Listing All Jobs <p>
            <% elsif params[:keywords].empty? %>
              Listing All Jobs in <%= params[:location] %>
            <% elsif params[:location].empty? %>
              Listing All Jobs for keyword(s):[<%= params[:keywords] %>]
            <% else %>
              <p class="brand"><%= "Listing jobs in " + params[:location] + " for keyword(s): " + params[:keywords] %></p> 
            <% end %>
          <% end %>
        </div>
      </div>
    </div>

    <div class="container-fluid">
        <div class="row-fluid">
          <div>
            <%= yield %>
          </div>
        </div><!--/row-->

      <footer>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
